export enum WebRole {
  USER = 1,
}

export enum App {
  GLOBAL = 'global',
}

export enum Access {
  CREATE = 'c',
  READ = 'r',
  UPDATE = 'u',
  DELETE = 'd',
}

export enum Feature {
  USER_DETAIL = 'detail',
  PROFILE = 'profile',
  NOTIFICATION = 'notification',
}
